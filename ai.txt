AI changes log

This file is managed by an automated assistant to record the intent of automated updates.

- Updated CMakeLists.txt to prefer vendored googletest when present.
- Ensured `main` links to `math_operations`.
- Created `ci.bat` for Windows CI and improved `ci.sh`.
- Added GitHub Actions workflow `.github/workflows/ci.yml` to build and run tests.

End of AI log.
